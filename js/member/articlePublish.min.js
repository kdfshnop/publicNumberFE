'use strict';

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

/*-----------------------------------------------------------------------------------------------------------------------------------------------------------------------
 1. 项目名称：有房有客微信CDN框架
 2. 页面名称：articlePublish.js (发布文章页)
 3. 作者：douyadong@lifang.com
 -----------------------------------------------------------------------------------------------------------------------------------------------------------------------*/
var articlePublishController = function (_Controller) {
    _inherits(articlePublishController, _Controller);

    function articlePublishController() {
        _classCallCheck(this, articlePublishController);

        var _this = _possibleConstructorReturn(this, (articlePublishController.__proto__ || Object.getPrototypeOf(articlePublishController)).call(this));

        _this.createArticle();
        return _this;
    }
    /*-----------------------------------------------------------------------------------------------------------------------------------------------------------------------
    生成文章
    -----------------------------------------------------------------------------------------------------------------------------------------------------------------------*/


    _createClass(articlePublishController, [{
        key: 'createArticle',
        value: function createArticle() {
            var self = this;
            var link = "mp.weixin.qq.com";
            var agentId = $.cookie('agentId');
            var openId = $.cookie('openId');
            $('.article-link>input').on('input', function () {
                if ($('.article-link>input').val()) {
                    $('.article-link>.clear').show();
                } else {
                    $('.article-link>.clear').hide();
                }
            });
            $('.article-link>.clear').click(function () {
                $('.article-link>input').val('');
                $('.article-link>.clear').hide();
            });
            $('.key>.btn').click(function () {
                var origin = $('.article-link>input').val();
                origin = $.trim(origin);
                if (!origin) {
                    $.tips('地址不能为空', 2);
                    return;
                } else if (origin.indexOf(link) <= -1) {
                    $.tips('地址格式不正确', 2);
                    return;
                } else {
                    // 发送请求;
                    $('.key>.btn').attr('disabled', true);
                    self.request(self.apiUrl.member.publishArticleCount, { origin: origin, openId: openId }, {
                        successCallback: function successCallback(data) {
                            // $('.key>.btn').attr('disabled',false);
                            self.request(self.apiUrl.member.publishArticleSuccess, { origin: origin, openId: openId }, {
                                successCallback: function successCallback(data) {
                                    // $.cookie('id',data.data.id);//文章id;
                                    location.href = "/member/article/reprint?id=" + data.data.id;
                                }
                            });
                        },
                        errorCallback: function errorCallback(data) {
                            $('.key>.btn').attr('disabled', false);
                            $.tips(data.message, 2);
                        },
                        exceptionCallback: function exceptionCallback(data) {
                            $('.key>.btn').attr('disabled', false);
                        }
                    });
                }
            });
        }
    }]);

    return articlePublishController;
}(Controller);
/*-----------------------------------------------------------------------------------------------------------------------------------------------------------------------
类的初始化
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------*/


$(document).ready(function () {
    new articlePublishController();
});
//# sourceMappingURL=articlePublish.min.js.map
